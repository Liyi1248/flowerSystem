此文件记笔记
A
1.style标签是否加上scope：加上scope，自定义的css样式有用，但是无法更改element中设置的默认样式（自带的样式）；
不加scope则反之，自定义的使用无效，但是更改element自带的默认样式是有效的









B
表管理：address.vue有个顾客收获地址表，收获地址存在个数限制，限制一页显示全部的数据






C过程记录
1.顾客个人中心，personInfo下面三个子路由：cusChangePwd,completeCusInfo,address
2.顾客订单中心：order一个子路由，order可显示已发货和待发货的订单数据（纯显示，无其他操作），历史订单具有表格基本的增删改查操作，所以另外一个组件实现
3.额外添加（为防遗忘：订单收货后，应该添加一个星级评价）
